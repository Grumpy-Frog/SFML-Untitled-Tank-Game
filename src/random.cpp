#include "random.h"
#include <bits/stdc++.h>

/*
int greaterRandom(int max)
{
    int randomNumber = rand();
    float random = (randomNumber % max)+1;
    int myRandom = random;
    return myRandom;
}

int greaterRandom0(int max)
{
    int randomNumber = rand();
    float random = (randomNumber % max)+1;
    int myRandom = random;
    return myRandom;

}

bool greaterRandomBool()
{
    int randomNumber = rand();
    float random = (randomNumber % 2)+1;
    int myRandom = random;
    if(myRandom == 1)
    {
        return true;
    }
    else
    {
        return false;
    }

}
*/

